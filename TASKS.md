# Follow-up Tasks

- [ ] Make the shared Property Pane visibility group functional in both Workbench and Storybook.
  Define what “Show in mobile and email view” changes in each preview, share the state model between
  hosts, preserve it across component and proxy-scenario remounts, and replace the Workbench-specific
  naming once the behavior is host-neutral.
