/**
 * Display mode for SPFx components
 * Matches the SPFx DisplayMode enum values
 */
export enum DisplayMode {
  /**
   * Read/Preview mode - component is displayed in read-only view
   */
  Read = 1,
  /**
   * Edit mode - component is displayed in editable view (default)
   */
  Edit = 2,
}
